/*A program to compute the factorial value of an input
more comment*/

void main(void){
  int x;
  int fac;
  x = input();
  fac = 1;
  fac = fac * x;

  while (x > 1){
    fac = fac * x;
    x = x - 1;
  }

  output(fac);
}
